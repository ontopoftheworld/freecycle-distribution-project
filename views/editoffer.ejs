<% include partials/header %>

    <div class="container">
        <h1>Edit Offer</h1>

        <form class="form-inline" action="/offers/<%= offer._id %>?_method=PUT" method="POST">
            <input type="text" name="offer[title]" value="<%= offer.title %>">
            <input type="text" name="offer[desc]" value="<%= offer.desc %>">
            <button>Update</button>
        </form>

    <a href="/offers/<%= offer._id %>">Go Back</a>
</div>

<% include partials/footer %>